{
  "name": "aligned-waters",
  "compatibility_date": "2026-07-30",
  // Cloudflare Pages config. `next build` with `output: "export"` writes the
  // static site here; Pages serves it and uses out/404.html for misses.
  "pages_build_output_dir": "./out"
}
